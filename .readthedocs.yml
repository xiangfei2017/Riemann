# .readthedocs.yml
version: 2

# 设置构建环境
build:
  os: ubuntu-22.04
  tools:
    python: "3.9"

# 使用 Sphinx 构建文档，并指定配置文件路径
sphinx:
  configuration: docs/source/conf.py  # 指向英文文档的 conf.py

# 安装 Python 依赖
python:
  install:
    - requirements: docs/requirements.txt